
1) Глобальная UI-архитектура (чтобы потом не переделывать)

✅ Canvas-слои

A) ShellCanvas — для экранов:
	•	Home
	•	Shop
	•	Settings
	•	Leaderboards

B) OverlayCanvas — для модалок поверх любого экрана:
	•	ConfirmDialog
	•	Toast/Popup
	•	Loading/Spinner
	•	(Позже) PurchaseDialog

Игровой UI у тебя уже есть, поэтому GameCanvas оставляем отдельно как сейчас.

✅ Общая рамка каждого Shell-экрана

Каждый экран строится одинаково:

ScreenRoot
├─ BackgroundLayer   (картинка/градиент)
├─ HeaderBar         (монеты / back / заголовок)
├─ ContentArea       (основное содержимое)
└─ FooterNav (опционально, если хочешь)


⸻

2) HOME / MAIN SCREEN — точная раскладка

Цель: 1 экран = 1 действие → Play, всё остальное вторично.

Background

✅ Главная картинка: большой арт стакана/фигур/неоновой темы
	•	на 60–80% высоты экрана
	•	слегка размыт или затемнён, чтобы кнопки читались
	•	внизу лёгкий градиент для контраста

Layout (портрет)

(1) Верхняя панель (HeaderBar)
	•	лево: кнопка профиля (Yandex) или “Guest”
	•	право: монеты + рекорд (две “пилюли”)
	•	💰 coins
	•	🏆 best

(2) Центральный CTA
	•	большая кнопка PLAY
	•	под ней (тонкий текст): “Tap bubbles • Make combos”

(3) Нижний блок кнопок (3 кнопки в ряд)
	•	SHOP (иконка корзины)
	•	LEADERS (иконка кубка)
	•	SETTINGS (иконка шестерёнки)

📌 Почему так?
	•	Play — главный
	•	Остальные действия не мешают

Layout (десктоп / широкие)
	•	фон растягивается
	•	контент остаётся в центре в max-width контейнере
	•	кнопки не “разъезжаются” по углам

⸻

3) ROUTING — как это правильно сделать (простая схема)

Экраны (stack)
	•	Home
	•	Shop
	•	Settings
	•	Leaderboards
	•	Game

Правило переходов
	•	Home → Game
	•	Home → Shop / Settings / Leaderboards
	•	Shop/Settings/Leaderboards → Back → Home
	•	Game → PauseOverlay → (Home / Settings / Restart / Resume)
	•	GameOverOverlay → (Retry / Home / Shop)

📌 ВАЖНО: Game — отдельная “ветка”, но возвращаемся всегда на Home.

Рекомендуемый Router API
	•	ShowScreen(ScreenId.Home)
	•	ShowScreen(ScreenId.Shop)
	•	PushOverlay(OverlayId.Confirm)
	•	PopOverlay()

Back/ESC:
	•	если overlay — закрыть overlay
	•	иначе если не Home — вернуться Home
	•	иначе — ничего

⸻

4) SHOP SCREEN — конкретная раскладка, без геймплей UI

Твой магазин должен быть двухэтажным:
	1.	Upgrades (основное)
	2.	Consumables (покупка предметов)

HeaderBar
	•	лево: Back
	•	центр: “Shop”
	•	право: 💰 coins

ContentArea = 2 вкладки или один скролл

Вариант A (лучше UX): Tabs
	•	UPGRADES | ITEMS
Удобно, не перегружает.

Вариант B: One scroll

Список апгрейдов сверху, потом разделитель, потом предметы.

Upgrades list (карточки)

Каждая карточка — одинаковая структура:

Слева: иконка апгрейда
Центр: название + текущий уровень
Низ: “Current → Next” (например +50% → +75%)
Справа: кнопка “UPGRADE 500” (цена)

Карточки:
	1.	Score Multiplier
	2.	Coin Multiplier
	3.	Bonus Drop Chance
	4.	Bonus Upgrades (особая карточка)

Bonus Upgrades — лучше сделать “Details”
	•	карточка ведёт на экран “Bonus Upgrades” (внутри Shop)
или
	•	раскрывающийся список уровней 0–7

✅ Я бы делал внутреннюю страницу, чтобы не захламлять.

ITEMS (Consumables)

2 карточки:
	•	Touch-to-kill (Buy 5000)
	•	Machine gun (Buy 5000)

На карточке:
	•	иконка
	•	цена
	•	“Owned: N”
	•	кнопка Buy

⸻

5) SETTINGS SCREEN — минимально и по делу

HeaderBar
	•	Back | Settings | (пусто)

ContentArea — секции

Audio
	•	Music slider
	•	SFX slider
	•	Mute toggle

Account (Yandex)
	•	статус: Guest / Logged-in
	•	кнопка Login / Logout

Data
	•	Reset progress (danger button, confirm dialog)
	•	Restore purchases

📌 Важно: Reset и Restore — внизу, отделить.

⸻

6) LEADERBOARDS SCREEN — All-time + Weekly

HeaderBar
	•	Back | Leaderboards | Refresh icon

Tabs
	•	ALL-TIME
	•	WEEKLY

List
	•	Top 10
	•	отдельная строка “You” (внизу или sticky)

📌 Weekly: прямо подписать период “Week #XX” или “This week”.

⸻

7) Какие картинки где ставить (чётко)

Home
	•	Main background art: стакан + фигуры + неон
	•	лёгкий blur/overlay
	•	логотип/название можно в Header или чуть выше Play

Shop
	•	фон тот же, но темнее/спокойнее
	•	небольшая декоративная иллюстрация вверху (не обязательна)
	•	иконки апгрейдов важнее

Settings
	•	простой фон, минимум графики
	•	маленькая иллюстрация “шестерёнка/неон” сверху (опционально)

Leaderboards
	•	фон темнее, акцент на таблице
	•	иконка кубка/звезда в заголовке

⸻

8) Мини-список “что точно нужно в EPIC-10”

Чтобы Codex реализовал App Shell быстро:

✅ Router/ScreenManager
✅ Screen prefabs: Home, Shop, Settings, Leaderboards
✅ HeaderBar component (Back/Title/Coins)
✅ Button styles + Icon buttons
✅ OverlayCanvas + ConfirmDialog
✅ Back/ESC handling
